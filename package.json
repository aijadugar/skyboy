{
  "name": "skyboy",
  "version": "0.1.0",
  "private": true,
  "description": "skyboy.in - a fast, curated, agent-agnostic directory of portable AI-era skills",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm --workspace apps/web run dev",
    "build": "npm --workspace apps/web run build",
    "start": "npm --workspace apps/web run start",
    "lint": "npm --workspace apps/web run lint",
    "validate-skills": "node --experimental-strip-types scripts/validate-skill.ts",
    "generate-manifests": "node --experimental-strip-types scripts/generate-manifest.ts",
    "export-catalog": "node --experimental-strip-types scripts/export-catalog.ts",
    "detect-duplicates": "node --experimental-strip-types scripts/detect-duplicates.ts",
    "dedupe": "node --experimental-strip-types scripts/detect-duplicates.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "drawably": "^0.3.10"
  }
}
