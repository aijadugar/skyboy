{
  "name": "skyboy",
  "version": "0.1.0",
  "description": "The skyboy CLI: add portable SKILL.md skills to your project with one command.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "skyboy": "./dist/index.js"
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@skyboy/core": "*",
    "@types/node": "^22.10.0"
  }
}
