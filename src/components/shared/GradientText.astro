---
import { cn } from "@/lib/utils";

export interface Props {
  as?: keyof HTMLElementTagNameMap;
  class?: string;
}

const { as: Tag = "span", class: className } = Astro.props;
---

<Tag class={cn("text-gradient-accent", className)}>
  <slot />
</Tag>
